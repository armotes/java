package com.pw.rpc.${projectName};

import java.util.List;
import com.pw.common.pojo.dto.PageDTO;
import com.pw.common.pojo.dto.org.CompanyDTO;
import com.pw.common.pojo.query.org.CompanyQuery;
import org.springframework.cloud.openfeign.FeignClient;
import org.springframework.web.bind.annotation.*;

/**
 * ${tableDesc} RPC服务
 * This code is generated by FreeMarker
 * @author luokai
 *
 */
@FeignClient("${applicationName}")
public interface ${class}Service {

    /**
     * 查询${tableDesc}列表
     * @param query
     * @return List<${class}DTO>
     */
    @PostMapping("/${projectName}/${class?uncap_first}/getList")
    List<${class}DTO> getList(@RequestBody ${class}Query query);

   /**
    * 查询${tableDesc}列表
    * @param query
    * @return Page<${class}DTO>
    */
    @PostMapping("/${projectName}/${class?uncap_first}/getPageList")
     PageDTO<${class}DTO> getPageList(@RequestBody ${class}Query query);

    /**
     * 新增${tableDesc}
     */
    @PostMapping("/${projectName}/${class?uncap_first}/insert")
     void insert(@RequestBody ${class}DTO ${class?uncap_first});

    /**
     * 删除${tableDesc}
     */
    @GetMapping("/${projectName}/${class?uncap_first}/deleteById/{<#list properties as property><#if  property.isKey == 1>${property.propertyName}</#if></#list>}")
     void deleteById( @PathVariable("<#list properties as property><#if  property.isKey == 1>${property.propertyName}</#if></#list>") <#list properties as property><#if  property.isKey == 1>${property.javaType} ${property.propertyName}</#if></#list>);

    /**
     * 通过id查询${tableDesc}
     * @param <#list properties as property><#if  property.isKey == 1>${property.propertyName}</#if></#list>
     * @return ${class}DTO
     */
    @GetMapping("/${projectName}/${class?uncap_first}/getById/{<#list properties as property><#if  property.isKey == 1>${property.propertyName}</#if></#list>}")
     ${class}DTO getById(@PathVariable("<#list properties as property><#if  property.isKey == 1>${property.propertyName}</#if></#list>") <#list properties as property><#if  property.isKey == 1>${property.javaType} ${property.propertyName}</#if></#list>);

    /**
     * 修改${tableDesc}
     */
    @PostMapping("/${projectName}/${class?uncap_first}/update")
     void update(@RequestBody ${class}DTO ${class?uncap_first});

}